<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/login_signup_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@android:color/white"
        android:elevation="10dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="@dimen/register_bottom_spacing">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Pas encore de compte ?"
            android:textColor="@android:color/black" />

        <TextView
            android:id="@+id/login_sign_up"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/register_small_spacing"
            android:layout_marginStart="@dimen/register_small_spacing"
            android:text="Inscription"
            android:textColor="@color/register_accent_color"
            android:textStyle="bold" />

    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/login_signup_container"
        android:background="@color/register_background_color"
        android:clipToPadding="false"
        android:padding="@dimen/register_screen_spacing">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:orientation="vertical">

            <com.rengwuxian.materialedittext.MaterialEditText
                android:id="@+id/login_username_edittext"
                style="@style/MaterialEditText"
                android:hint="login"
                android:imeOptions="actionNext"
                android:inputType="textEmailAddress"
                android:nextFocusForward="@+id/login_password_edittext" />

            <com.rengwuxian.materialedittext.MaterialEditText
                android:id="@+id/login_password_edittext"
                style="@style/MaterialEditText"
                android:layout_marginTop="@dimen/register_standard_spacing"
                android:hint="password"
                android:imeOptions="actionDone"
                android:inputType="textPassword" />

            <Button
                android:id="@+id/login_sign_in_button"
                style="@style/RegisterButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/register_standard_spacing"
                android:text="Login" />

            <TextView
                android:id="@+id/login_forgot_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/register_standard_spacing"
                android:text="Mot de passe oublié ?"
                android:textColor="@color/register_secondary_text_color" />

        </LinearLayout>

    </ScrollView>

</RelativeLayout>
